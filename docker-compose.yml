version: '3.8'

services:
  whatsapp-scheduler:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - TZ=Europe/Paris
      - ANTI_SPAM_DELAY=15
    restart: unless-stopped
    shm_size: '512mb'
